import sys
sys.stdin = open('input.txt', 'r')

# useful for competion programming, write cmd input in a text file and run it like you would
#

T = int(raw_input("")) # taken from text, but acts like from input
